/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes añadir otras configuraciones de Next.js en el futuro
  // si las necesitas (por ejemplo, para imágenes, redirecciones, etc.).
};

module.exports = nextConfig;